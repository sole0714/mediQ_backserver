spring:
  application:
    name: mediQBack

  # 🚨 mail이 application과 같은 라인에 있어야 합니다!
  mail:
    host: smtp.gmail.com
    port: 587
    username: 내꺼@gmail.com
    password: 내꺼
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true
          auth: true
          timeout: 5000

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: "jdbc:mariadb://192.100.100.10:3306/web"
    username: root
    password: qwer1234

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      springframework:
        security:
          web:
            FilterChainProxy: debug
        web:
          servlet:
            DispatcherServlet: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace